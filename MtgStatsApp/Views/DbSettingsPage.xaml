<Page x:Class="MtgStatsApp.Views.DbSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:MtgStatsApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Database Connection Settings">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock FontSize="28" FontWeight="SemiBold" Text="Database Connection" />
            <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                       Text="Choose an existing MTG Stats SQLite database file, or create a new one." Margin="0,4,0,0" />
        </StackPanel>

        <ui:Card Grid.Row="1" Padding="24" HorizontalAlignment="Left" Width="700">
            <StackPanel Spacing="16">
                <TextBlock FontSize="20" FontWeight="SemiBold" Text="Database File Path" />
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ui:TextBox x:Name="TxtDbPath" Grid.Column="0" 
                                PlaceholderText="C:\Users\...\MTG_Decks\collection-notes\mtg_stats.db"
                                VerticalAlignment="Center" IsReadOnly="True" Margin="0,0,8,0"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                        <ui:Button Content="Browse..." Click="OnBrowseClick"/>
                        <ui:Button Content="Create New..." Click="OnCreateNewClick"/>
                    </StackPanel>
                </Grid>

                <ui:InfoBar x:Name="InfoBarConnection" IsOpen="False" Title="Connection Info" Message="Please select a valid database." Severity="Informational" Margin="0,8,0,0"/>

                <ui:Button x:Name="BtnConnect" Content="Connect and Proceed" Appearance="Primary" 
                           Click="OnConnectClick" Width="180" HorizontalAlignment="Right" Margin="0,16,0,0"/>
            </StackPanel>
        </ui:Card>
    </Grid>
</Page>
